"use client";

// Imports agora compilam:
// import { User, UserAPI, UserRole, UserStatus, signInWithPassword, signUp, signOut as supabaseSignOut } from "@/lib/api";

// ... resto inalterado, usando as funções exportadas